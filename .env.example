# ============================================
# 0xRIP 环境变量示例
# 复制本文件为 .env 并填入真实值。不要将 .env 提交到 Git。
# ============================================

# --------------------------------------------
# OpenAI 兼容 API（LLM / VLM / Voice）
# 用于：引导仪式、墓志铭、招魂对话、时间线加工、URL 处理等
# --------------------------------------------

# API 基础地址。使用 OpenAI 官方时留空或填 https://api.openai.com
# 使用 Azure / 国内中转 / 本地部署时填对应地址，例如：
#   Azure: https://your-resource.openai.azure.com/openai/deployments/your-deployment
#   中转:   https://api.xxx.com/v1
#   本地:   http://localhost:8000/v1
OPENAI_COMPATIBLE_BASE_URL=

# API Key。OpenAI 在 https://platform.openai.com/api-keys 创建
# Azure 等使用其各自的 Key 格式
OPENAI_API_KEY=

# --------------------------------------------
# 可选：模型名称（不填则使用后端默认）
# --------------------------------------------

# 对话/文本生成（引导、墓志铭、招魂、时间线、意图解析）
# 例：gpt-4o, gpt-4o-mini, gpt-4-turbo
OPENAI_CHAT_MODEL=

# 视觉模型（看图写墓志铭等）。例：gpt-4o, gpt-4-vision
OPENAI_VISION_MODEL=

# 语音合成 TTS。例：tts-1, tts-1-hd
OPENAI_TTS_MODEL=

# 语音识别 STT。例：whisper-1
OPENAI_STT_MODEL=

# --------------------------------------------
# 可选：功能开关
# --------------------------------------------

# 是否启用 TTS（引导/幽灵出声）。true | false，不填默认 false
ENABLE_TTS=false

# 是否启用 STT（语音输入）。true | false，不填默认 false
ENABLE_STT=false

# 无 API Key 时是否降级为固定文案（不调接口）。true | false，不填默认 true
FALLBACK_WITHOUT_API=true
