# 示例输出

这个目录里的文件全部是**虚构、脱敏、不可回溯到真实个人**的演示样例。

用途：

- 让 GitHub 访问者快速理解 skill 的实际产出长什么样
- 给后续 prompt 调整提供一个稳定的对照样本
- 避免把真实用户的数字痕迹提交到仓库

当前包含两组样例：

- `legacy_audit_demo.json` / `legacy_audit_demo.md`
- `ai_clone_demo.json` / `ai_clone_demo.md`
